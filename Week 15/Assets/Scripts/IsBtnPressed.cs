using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IsBtnPressed : MonoBehaviour
{
    // Start is called before the first frame update

    public bool isPressed;

    void Start()
    {
        isPressed = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
